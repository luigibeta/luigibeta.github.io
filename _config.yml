title: Luigi Beta
description:
markdown: kramdown

collections:
  posts:
    permalink: /blog/:year/:month/:day/:slug/
    layout: post

defaults:
  - scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
